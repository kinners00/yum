{
  "description": "Update security packages via Yum",
  "input_method": "environment",
  "parameters": {
    "security": {
      "description": "Minimal or full security update",
      "type": "Enum[minimal, full]"
    }
  }
}
